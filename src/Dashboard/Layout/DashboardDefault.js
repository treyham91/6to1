import React from 'react';

const DashboardDefault = (props) => {
    return (
        <React.Fragment>
            {props.children}
        </React.Fragment>
    )
}

export default DashboardDefault;